[workspace]
members = [
  "apps/cli",
  "crates/core",
  "crates/renderer-json",
  "crates/tui",
]
resolver = "2"

[profile.release]
# Modern yet stable release profile tuned for small binaries
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
