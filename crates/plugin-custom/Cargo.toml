[package]
name = "pendon-plugin-custom"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "TOML-driven custom plugin for Pendon"

[dependencies]
pendon-core = { path = "../core" }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
