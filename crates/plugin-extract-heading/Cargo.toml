[package]
name = "pendon-plugin-extract-heading"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Collect heading metadata for Pendon"

[dependencies]
pendon-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
