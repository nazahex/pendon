[package]
name = "pendon-plugin-anchor"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Extended anchor/link processor for Pendon"

[dependencies]
pendon-core = { path = "../core" }
pendon-renderer-solid = { path = "../renderer-solid" }
regex = "1"
serde_json = "1"
